repos:
  - repo: local
    hooks:
      - id: public-boundary
        name: reject private directory paths
        entry: python scripts/check_public_boundary.py
        language: system
        pass_filenames: false
        always_run: true
